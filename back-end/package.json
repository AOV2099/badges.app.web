{
  "name": "badges",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "sh -c 'set -a; [ -f ../.env ] && . ../.env; set +a; nodemon src/index.js'",
    "dev:badges": "sh -c 'set -a; [ -f ../.env ] && . ../.env; set +a; PORT=3001 nodemon src/index.js'",
    "start:badges": "sh -c 'set -a; [ -f ../.env ] && . ../.env; set +a; PORT=3001 node src/index.js'",
    "start": "sh -c 'set -a; [ -f ../.env ] && . ../.env; set +a; node src/index.js'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jose": "^6.2.3",
    "pg": "^8.22.0",
    "swagger-ui-express": "^5.0.1",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
